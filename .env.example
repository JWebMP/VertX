# Runtime
PORT=8080
ENVIRONMENT=dev
BASE_URL=http://localhost:8080

# Observability (optional)
TRACING_ENABLED=false
LOGGING_BACKEND=log4j2
METRICS_EXPORT=disabled

# Auth (optional; required only if securing Vert.x endpoints)
OAUTH2_ISSUER_URL=
OAUTH2_CLIENT_ID=
OAUTH2_CLIENT_SECRET=
JWKS_URI=
